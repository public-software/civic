# Machine-readable entry for this repository. Aggregated into public-software/catalog.
# Edit here; `pub catalog sync` propagates description, topics and custom properties to GitHub.

[repo]
name        = "civic"
ring        = "domain"          # spine | platform | system | domain | standards
layers      = ["L16"]
wave        = 1
tier        = "incubating"        # incubating | stable | core | archived
maintainers = ["maint-civic"]
summary     = "Rules engine (tax/benefits), rules-as-code corpus tooling, LMS, elections, legal research."

# One [[component]] per crate, app or service this workspace publishes.
# planned: pub-rules · corpus tooling · pub-lms · pub-vote · pub-law
#
# [[component]]
# crate     = "pub-civic-example"
# kind      = "lib"                # lib | app | service | plugin | spec
# ledger    = "<ledger entry name>"
# readiness = "none"               # shipped | partial | seed | none
# effort    = 3
# specs     = []
# provides  = ["public:area/iface@1"]
# requires  = []
